node_modules/
tokens/.figma-dump.json
tokens/.figma-descriptions.json
tokens/.dump-*.json
.DS_Store
storybook-static/
build/

# Storybook build log — regenerated by `npm run build-storybook`
build-storybook.log

# Generated diagnostic. Its content depends on whether the consuming site repo
# is checked out alongside this one, so it churns and cannot be reproduced in CI.
dist/report.html
